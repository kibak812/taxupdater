# 이메일 알림 설정 예시
# 이 파일을 .env로 복사하고 실제 값을 입력하세요

# Email Configuration
# 이메일 비밀번호 (Gmail의 경우 앱 비밀번호 사용 권장)
EMAIL_PASSWORD=your_email_password_here

# 대시보드 URL (이메일에서 링크로 사용)
DASHBOARD_URL=https://taxupdater-monitor.loca.lt

# Optional: 여러 이메일 계정 사용 시
# EMAIL_PASSWORD_admin=admin_email_password
# EMAIL_PASSWORD_notification=notification_email_password

# 주요 이메일 서비스 SMTP 설정 참고:
# 
# Gmail:
# - SMTP 서버: smtp.gmail.com
# - 포트: 587 (TLS) 또는 465 (SSL)  
# - 2단계 인증 활성화 후 앱 비밀번호 생성 필요
#
# Naver:
# - SMTP 서버: smtp.naver.com
# - 포트: 587 (TLS) 또는 465 (SSL)
# - IMAP/SMTP 설정에서 사용 허용 필요
#
# Outlook/Hotmail:
# - SMTP 서버: smtp-mail.outlook.com
# - 포트: 587 (TLS)
# - 2단계 인증 활성화 후 앱 비밀번호 생성 필요

# 추가 환경 변수 (필요 시)
# LOG_LEVEL=INFO
# DATABASE_PATH=data/tax_data.db